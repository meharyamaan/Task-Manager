OWERNER
USAMA YAMAAN
usama.yamaan@gmail.com
Clone Repository

==============Task Manager Frontend===========
Go TO
cd/frontend

1)Getting Started
To get the frontend up and running on your local machine, follow these steps:

2)Prerequisites
Make sure you have the following installed:

Node.js
npm (Node Package Manager) or yarn

3)Running the Application
npm start
# OR
yarn start

==============Task Manager Backend=============
Go TO
cd/backend
Getting Started
To get the project up and running on your local machine, follow these steps:

Prerequisites
Make sure you have the following installed:

Node.js
npm (Node Package Manager) or yarn
MySQL (Make sure the MySQL server is running)


1)Install dependencies:
npm install
OR
yarn install


2)Set up the database:

->Create a MySQL database for the project.

->Configure the database connection settings in a .env file at the root of your project. Here is an example .env file configuration
{DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=your db name
DB_PORT=3306
}
Replace your_mysql_password with your actual MySQL password.

3)to start
npm start
node src/app.js
